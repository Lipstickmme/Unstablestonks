// External trading surfaces the terminal links out to.
//
// BaseBot is a Telegram trading bot on Base. The link carries our referral code,
// so it is a plain outbound link — the terminal never brokers the trade, never
// holds funds, and nothing about the user's wallet is passed along.
export const BASE_BOT_URL = "https://t.me/based_eth_bot?start=r_5092368318" as const;
